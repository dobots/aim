// These are configurable options
// e.g. add to cmake -DBUILD_EIGEN:BOOL=ON to enable sparse matrices
// you can also disable them here by adding "0" this will overwrite command-line options

#cmakedefine BUILD_EIGEN
